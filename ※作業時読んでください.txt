githubが入っているので、更新したらターミナルで

git add 更新したファイル名1
git add 更新したファイル名2
git add 更新したファイル名3　←全部やる
・
・
・
git commit -m "コメント"　←区切りのいいところで「日付-PC完了」「日付-レスポンシブ完了」等
git push origin master


の操作をしてください。
githubのほうで作業を蓄積できるので、バックアップなどは不要です。